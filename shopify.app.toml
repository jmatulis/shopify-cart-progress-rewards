# Learn more about configuring your app at https://shopify.dev/docs/apps/tools/cli/configuration

client_id = "c5db19d7e4c48f386fb3e9be25578d9e"
application_url = "https://shopify.dev/apps/default-app-home"
embedded = true
name = "Store Project"

[access_scopes]
# Learn more at https://shopify.dev/docs/apps/tools/cli/configuration#access_scopes
scopes = ""

[webhooks]
api_version = "2026-10"

  [[webhooks.subscriptions]]
  uri = "/webhooks/app/uninstalled"
  topics = [ "app/uninstalled" ]

  [[webhooks.subscriptions]]
  uri = "/webhooks/app/scopes_update"
  topics = [ "app/scopes_update" ]

[auth]
redirect_urls = [ "https://shopify.dev/apps/default-app-home/api/auth" ]

[build]
automatically_update_urls_on_dev = true
# The app URL is managed by `shopify app dev` (a tunnel that changes every run),
# so deploys ship extensions only and leave URLs/webhooks untouched. Set this to
# true once the app is hosted at a stable URL.
include_config_on_deploy = false
